/**
 * mul - multiplies two number and returns their product.
 * @a: The first operand to be multiplied.
 * @b: The second operand to multipied.
 *
 * Return: The product of a and b
 */
int mul(int a, int b)
{
	return (a * b);
}
