#include <stdio.h>

/**
 * main - Entry point
 * @str[]: string or array of characters to be printed
 *
 * Description: Prints a string using the puts() function
 * Return: Always 0 (Succes)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
